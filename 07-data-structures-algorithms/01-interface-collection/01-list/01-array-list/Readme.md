### ArrayList

ArrayList is a `resizable array` implementation of the `List interface` in Java. It is part of java.util package and allows dynamic memory allocation unlike standard arrays.

- Allows duplicates
- Maintains insertion order
- Not synchronized (not thread-safe by default)
- Implements RandomAccess (fast indexed access)
