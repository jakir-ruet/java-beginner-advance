# Default: normalize text files and check out with platform-native line endings
* text=auto

# Unix/Linux shell scripts and Maven wrapper
*.sh text eol=lf
mvnw text eol=lf

# Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
